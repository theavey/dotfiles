# alias to easily add aliases
alias realias='$EDITOR ~/.dotfiles/washington/.zsh_aliases; source ~/.zshrc'

alias vpn='/opt/cisco/anyconnect/bin/vpn'
alias vpnui='/opt/cisco/anyconnect/bin/vpnui'

alias seeding='transmission-remote -l | grep Seeding'
alias tlist='transmission-remote -l'
alias tmr='transmission-remote '

alias open='xdg-open'

alias lh='ls -lrth'
alias la='ls -A'

# start iterm2 shell integration
alias shelli='source ~/.iterm2_shell_integration.zsh'

